# Railway: use Dockerfile for reliable better-sqlite3 native builds.
# Attach a volume mounted at /data (see README).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/"
healthcheckTimeout = 30
